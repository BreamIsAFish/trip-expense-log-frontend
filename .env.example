# Public API base URL (must include /api). For Vite dev with proxy, leave unset to use /api on the dev server.
VITE_API_URL=http://localhost:8080/api

# LINE LIFF ID from LINE Developers Console
VITE_LIFF_ID=
